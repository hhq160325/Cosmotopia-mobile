# Cosmotopia-mobile
mobile  app of  Cosmotopia systerm
add to  terminal:
npm install 
npm install --save-dev expo-module-scripts
npm install -g expo-cli
rm -rf node_modules package-lock.json
npm install --save-dev @types/constants
npm install expo-font@latest
npm install @react-navigation/native @react-navigation/stack
npm install --save-dev expo-module-scripts
npm install @expo/config-plugins@7.2.2 expo-constants@~14.4.2
npx expo install --fix
rm -rf node_modules package-lock.json
